import { Mesh, Vector3 } from 'three';
import { Context } from '../../base-types';
import { IfcClipper } from '../display';
export declare class VertexGenerator {
    private context;
    private clipper;
    cv: any;
    minimumOffset: number;
    buckets: {
        mesh: Mesh;
        row: number;
        column: number;
    }[];
    bucketsOffset: Vector3;
    polygons: number[][];
    private currentBucketIndex;
    private dims;
    private readonly htmlImage;
    constructor(context: Context, clipper: IfcClipper);
    initializeOpenCV(openCV: any): void;
    getVertices(bucketWidth: number): Promise<void>;
    private getModel;
    private getDimensions;
    private getSizeAndCenter;
    private renderBucket;
    private getEdges2DPoints;
}
