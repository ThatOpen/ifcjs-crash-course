import { BufferGeometry, Group, Material, Mesh, Plane, PlaneGeometry } from 'three';
import { Context } from '../../../base-types';
export declare class IfcStencil {
    static stencilGeometry: PlaneGeometry;
    root: Group;
    stencilGroup: Group;
    stencilObject: Mesh<BufferGeometry, Material | Material[]>;
    renderOrder: number;
    stencilMaterial: Material;
    stencilMesh: Mesh;
    private context;
    private readonly plane;
    constructor(context: Context, plane: Plane, planeMaterial?: Material);
    setupStencil(mesh: Mesh): void;
    updateStencilMaterial(): void;
    private getStencilMesh;
    private setupPlaneMaterial;
    private createPlaneStencilGroup;
}
